# Dependencies
node_modules/
**/node_modules/

# Build output
.next/
dist/
.turbo/
*.tsbuildinfo

# Logs / OS
*.log
.DS_Store

# Test coverage
coverage/

# Local-only env overrides (real .env files ARE committed intentionally:
# apps/api/.env and database/.env hold the local-dev connection config the
# bring-up recipe needs to boot from a fresh clone)
.env.*.local
